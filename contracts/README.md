# Hardhat Titan-contracts

This is project for Titan embedded contracts.

1. `yarn install`
2. Compile the contracts, export `.bin` and `.abi` files:

   ```shell
   yarn compile
   ```

3. Copy those files to corresponding directories in [x/pointer/artifacts/](../x/pointer/artifacts/)
